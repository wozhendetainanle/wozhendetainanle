name: Draw contribution animation

on:
  schedule:
    - cron: "17 0 * * *"
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: write

concurrency:
  group: contribution-snake
  cancel-in-progress: true

jobs:
  draw:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Generate contribution animations
        uses: Platane/snk@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/contribution-loop.gif?color_snake=#AD7A62&color_dots=#D8D4CC,#CFDAD5,#A9C2B9,#6B8583,#4E6361&color_background=#EFECE6
            dist/contribution-loop-dark.gif?color_snake=#B3A075&color_dots=#2A2D2C,#36433F,#4E6361,#6B8583,#A9C2B9&color_background=#161817

      - name: Publish generated files to the output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          build_dir: dist
          target_branch: output
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
